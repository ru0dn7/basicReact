import clock from './clock.png'

function MyClockImages() { 

  return (
    <div>
      <img src={clock} alt='clock' style={{width:'30%', margin:'0 auto'}}/ >
    </div>
  );
};

export default MyClockImages;